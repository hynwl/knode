# AgentCanvas 가 내보낸 환경변수 목록. `.env` 로 복사한 뒤 값을 채우세요.
# crew.py 는 load_dotenv() 로 이 파일을 읽습니다. `.env` 는 절대 커밋하지 마세요.
{% if keys %}
{% for name, purpose in keys %}

# {{ purpose }}
{{ name }}=
{% endfor %}
{% else %}

# 이 크루에는 API 키가 필요하지 않습니다 (예: 로컬 Ollama 전용).
{% endif %}
